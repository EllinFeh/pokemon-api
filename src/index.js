import React from 'react';
import ReactDOM from 'react-dom/client';
import Pokeworld from './components/home/Homepage';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <Pokeworld />
  </React.StrictMode>
);
